import React, { Component } from 'react';
import {
  AppRegistry,
  StyleSheet,
  Text,
  ScrollView
} from 'react-native';

class Pokelist extends Component {

  /* Constructor */
  constructor(props) {
    super(props);
  }

  /* Render */
  render() {
    return (
      <ScrollView>
      </ScrollView>
    );
  }

}

export default Pokelist
